#include <vector>
#include <map>
#include <string>
using namespace std;
std::vector<vector<string>> check_in_config(unsigned char name[],unsigned short qtype);
void get_total(char* file_name );
char * get_config_server();